(function() {
  'use strict';

  describe('Array  Class', function() {
    // Tests for hasVowels()

    var names = ['stephen', 'segun', 'Toke', 'Ali', 'Felix', 'Scofield', 'Peter'];

    names.findNumber();

    //  it('Should return true for "Queueing"', function() {
    //         expect('Queueing'.hasVowels()).toBe(true);
    //       });
     it('', function() {
          expect(names.findNumber()).toEqual(false);
        });

          it('', function() {
             var names = [5, 'stephen', 'segun', 'Toke', 'Ali', 'Felix', 'Scofield', 'Peter'];

          expect(names.findNumber()).toEqual(5);
        });
  });


})();
